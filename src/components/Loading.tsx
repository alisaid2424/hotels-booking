const Loading = () => {
  return (
    <div className="absolute inset-0 element-center h-screen bg-slate-200/20 backdrop-blur-sm">
      <div className="loader animate-load" />
    </div>
  );
};

export default Loading;
